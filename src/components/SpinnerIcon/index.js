import React from 'react';
import Page from './page';
import './styles.css';

const Spinner = props => {
    return (
        <div className = "Spinner">
            <Page/>
        </div>
    )
}

export default Spinner